<%= form_for(private_place) do |f| %>
  <% if private_place.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(private_place.errors.count, "error") %> prohibited this private_place from being saved:</h2>

      <ul>
      <% private_place.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
